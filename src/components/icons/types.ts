import { type ColorValue } from 'react-native';

export type IconProps = {
  size?: number;
  color?: ColorValue;
  children?: never;
};

export const DEFAULT_COLOR = '#0C0C0D';
export const DEFAULT_SIZE = 24;
